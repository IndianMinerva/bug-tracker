#### Swagger UI: http://localhost:8080/swagger-ui.html

### How to run
execute `h run.sh` and then the application could be accessed from the swagger-url

Need to write a lot of test cases and upgrade the swagger to 3 still